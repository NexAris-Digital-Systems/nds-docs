# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  commands:
    - sphinx-build -b html -d _build/doctrees -T -v . _build/html


sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt

